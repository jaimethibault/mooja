<% content_for(:navbar_class, "navbar_home") %>

<div class="banner">
  <div class="row full-width">
    <div class="col-xs-6 col-xs-offset-3">
      <div class="banner-content">
        <h3>SURF EVERYWHERE, CHEAPER</h3>


            <div class= "search-home-btn btn btn-primary">
                <%= link_to "Check deals", surfcamps_path %>
            </div>

        </div>
    </div>
  </div>
</div>
</div>


<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="surfcamp-card-pic-wrapper">
        <% @surfcamps.each do |surfcamp| %>
        <div class="surfcamp-card-pic">
          <% if surfcamp.photo? %>
           <%= link_to cl_image_tag(surfcamp.photo.path, width: 510, height: 350, crop: :lfill, fetch_format: :auto), surfcamp_path(surfcamp) %>
                <%#= cl_image_tag surfcamp.photo.path, width: 510, height: 350, crop: :lfill, fetch_format: :auto %>

          <% end %>
            <div class="surfcamp-card-location">
              <%= surfcamp.name %>
            </div>
        </div>
 <% end %>
        </div>
    </div>
   <!--  <div class="col-xs-12 col-sm-4">
        <%#=cl_image_path surfcamp.photo.path, width: 510, height: 350, crop: :lfill, fetch_format: :auto %>

      </div>
    </div>
    <div class="col-xs-12 col-sm-4">

        <%#=  cl_image_path surfcamp.photo.path, width: 510, height: 350, crop: :lfill, fetch_format: :auto %>

      </div> -->
    </div>
  </div>



